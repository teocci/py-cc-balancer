# RELEASE

Release notes per version. Tag `vX.Y.Z` to trigger the GitHub Release workflow (Phase 10).

## Unreleased (0.1.0)

- Initial scaffold and phased implementation in progress. No release published yet.
