# I-2 — Additional exchanges

- **Improvement ID:** I-2
- **Version:** backlog (unscheduled)
- **Status:** 📋 BACKLOG — OKX delivered; more venues remain.
- **Related work:** OKX support landed in the Auth phase (Bybit/Binance/OKX now supported).

## Objective

Broaden venue coverage beyond the current Bybit/Binance/OKX set so the rebalancer can run
against more of the exchanges ccxt already supports.

## Approach

- OKX has been added — `SUPPORTED_EXCHANGES` now covers Bybit, Binance, and OKX, each with
  a quirks row.
- For each further venue: add it to `SUPPORTED_EXCHANGES` and add a per-venue quirks row
  (`stores/exchange_quirks.py`) capturing auth requirements, order flags, and any
  market/precision peculiarities.
- ccxt supports many more exchanges; promote specific ones as demand warrants rather than
  enabling them wholesale.

## Files changed

*(to be filled at finalization)*

## Verification

*(to be filled at finalization)*
