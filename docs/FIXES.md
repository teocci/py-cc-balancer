# FIXES

Bug-fix log. One row per fix; full detail in `docs/fixes/F-<N>.md`.

| ID | Symptom | Root cause | Fix | Phase |
|----|---------|-----------|-----|-------|
| [F-1](fixes/F-1.md) | All authenticated Bybit calls fail with `retCode 10002` (server timestamp / `recv_window`) when the local clock drifts past 5 s; `status`/`orders`/`cancel`/`performance`/`rebalance` exit 3. | `ExchangeStore._build_client` built the ccxt client without `adjustForTimeDifference`, so requests were signed with the unsynced local clock. | Set `options.adjustForTimeDifference = True` on the ccxt client; ccxt syncs to the exchange clock during `load_markets` (invoked by every unified private call). | 11 |
| [F-2](fixes/F-2.md) | `auth login` saves the profile but the credential check fails (`retCode 10003 "API key is invalid"`, exit 3) for valid **mainnet** keys; `auth status` reports `valid:null`. Could only be worked around with an explicit `--no-testnet`. | `_cmd_auth_login` hardcoded `testnet = DEFAULT_TESTNET if args.testnet is None else args.testnet`, ignoring `CCB_TESTNET` and TOML `[global] testnet` — unlike every other command. A mainnet key thus always landed in a sandbox profile and was verified against the testnet, which rejects it. | Added `config.resolve_login_testnet()` applying the app-wide precedence (flag > `CCB_TESTNET` env > TOML `[global] testnet` > default); `auth login` now calls it. The safety default (testnet) is unchanged when nothing is configured. | Auth |
| [F-3](fixes/F-3.md) | `auth login --exchange okx --key K --secret S` fails the credential check (exit 3) for valid **live** OKX keys; `auth status` reports `credentials: invalid`. | OKX needs a passphrase (`OK-ACCESS-PASSPHRASE`) on every private request, but `_collect_credentials` returned before prompting whenever key+secret were both present, so the flag/env path saved `password=None`; there was also no `CCB_PASSPHRASE` env var. (Compounded by the testnet safety default verifying live keys against OKX demo.) | Resolve the passphrase independently of key/secret; add `CCB_PASSPHRASE`; fail fast in `require_credentials`; print testnet/passphrase hints on a failed check. | Auth |
| [F-4](fixes/F-4.md) | OKX `auth login` (any OKX `load_markets`) crashes with `TypeError: '<' not supported between instances of 'NoneType' and 'str'` in ccxt `keysort`; intermittent (only when OKX lists a preopen instrument). | ccxt `4.4.94` bug: OKX "preopen" placeholders have an empty `instId`, so `okx.parse_market` yields `id=None`; `set_markets` stores it under a `None` key in `markets_by_id` (no None-guard) and `keysort` raises comparing `None` to string ids. Fixed upstream in ccxt `v4.5.57` (#28825). | Bump ccxt pin `4.4.94` → `4.5.65` (no `ccbalancer` code change); regenerate the changelog-impact digest. | Auth |
| [F-5](fixes/F-5.md) | Switching accounts across venues silently corrupts P&L: the global `portfolio.json`/`state.json`/`ledger.jsonl`/`decision_log.jsonl`/`flags.json` are shared, so `auth use <other> && rebalance`/`performance` rebalances one account's targets against another account's balances and interleaves fills from different accounts in the cost-basis ledger. | All per-account data files live once under `~/.ccbalancer/` with no account scoping; the active-account/`--account` selector only swaps credentials + venue, not the book. | Give each account its own `accounts/<id>/` data dir (keyed by a stable local id), routing the six book files through `AppConfig.data_dir`; one-time guarded migration of legacy root files (delivered by I-8). | 0.2.0 |
